# 19장-(2) 홈페이지 구현

```jsx
var http = require('http');
var fs = require('fs');
var url = require('url');

var app = http.createServer(function(request,response){
    var _url = request.url;
    var queryData = url.parse(_url, true).query;
    var pathname = url.parse(_url, true).pathname;
    var title = queryData.id; //

    if(pathname === '/'){
        var template = `
        <!doctype html>
        <html>
        <head>
          <title>WEB1 - ${title}</title>
          <meta charset="utf-8">
        </head>
        <body>
          <h1><a href="/">WEB</a></h1>
          <ul>
            <li><a href="/?id=HTML">HTML</a></li>
            <li><a href="/?id=CSS">CSS</a></li>
            <li><a href="/?id=JavaScript">JavaScript</a></li>
          </ul>
          <h2>${title}</h2>
          <p>${description}</p>
        </body>
        </html>
        `;
        response.writeHead(200);
        response.end(template);
    }
else {
  response.writeHead(404);
    response.end('not found');
}
});
app.listen(3000);
```

현재 코드에서 node main.js 실행시 홈페이지(web)으로 이동했을 때에 undefined라고 뜸 → 본문이 없다는 의미(홈에서도 pathname 은 ‘/’이다)

home에서의 pathname과 css,html,javascript 창에서의 pathname이 동일→ 홈페이지와 각 페이지가 구별이 안됨



> <aside> 💡 home에서는 쿼리스트링 존재x
>
> 다른 페이지에서는 쿼리스트링 존재o
>
> 쿼리스트링이 존재하면 ${[queryData.id](http://queryData.id)}가 값을 가짐
>
> →이거 이용하자!



```jsx
 if(pathname === '/'){
   if([queryData.id](<http://querydata.id/>) === undefined){
         var title = 'welcome';
         var description = 'hello, node.js';
     //<~>
}
   else{//쿼리스트링이 존재할 때}
}else{//이상한 값일 때
}
```

undefined → 정의되지 않은 데이터라는 의미

고로 pathname이 /인데 쿼리스트링 존재하지 않으면 즉, home페이지라면 title은 welcome, description은 hello, node.js로 주어서 홈페이지 구현하기

```jsx
var http = require('http');
var fs = require('fs');
var url = require('url');

var app = http.createServer(function(request,response){
    var _url = request.url;
    var queryData = url.parse(_url, true).query;
    var pathname = url.parse(_url, true).pathname;

    if(pathname === '/'){
      if(queryData.id === undefined){
              var title = 'Welcome';
              var description = 'Hello, Node.js';
              var template = `
              <!doctype html>
              <html>
              <head>ㅋㅋ
                <title>WEB1 - ${title}</title>
                <meta charset="utf-8">
              </head>
              <body>
                <h1><a href="/">WEB</a></h1>
                <ul>
                  <li><a href="/?id=HTML">HTML</a></li>
                  <li><a href="/?id=CSS">CSS</a></li>
                  <li><a href="/?id=JavaScript">JavaScript</a></li>
                </ul>
                <h2>${title}</h2>
                <p>${description}</p>
              </body>
              </html>
              `;
              response.writeHead(200);
              response.end(template);
      } else {
      fs.readFile(`data/${queryData.id}`, 'utf8', function(err,description){
        var title = queryData.id;
        var template = `
        <!doctype html>
        <html>
        <head>
          <title>WEB1 - ${title}</title>
          <meta charset="utf-8">
        </head>
        <body>
          <h1><a href="/">WEB</a></h1>
          <ul>
            <li><a href="/?id=HTML">HTML</a></li>
            <li><a href="/?id=CSS">CSS</a></li>
            <li><a href="/?id=JavaScript">JavaScript</a></li>
          </ul>
          <h2>${title}</h2>
          <p>${description}</p>
        </body>
        </html>
        `;
        response.writeHead(200);
        response.end(template);
      });
    }
  } else {
  response.writeHead(404);
    response.end('Not found');
}
});
app.listen(3000);
```

